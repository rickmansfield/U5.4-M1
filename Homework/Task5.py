# see Tom's solutions under assets

def solution(graph):
    target = len(graph) - 1
    result = []
    s = [[0]]
    path = s.pop()
    node path[-1]
    
    #  have I reached the end?
    for i in graph:
        s.append(path + [item])